/Users/whit3rabbit/Documents/GitHub/zigcat/docker-tests/logs/*/*.log {
    size 100M
    rotate 10
    compress
    delaycompress
    missingok
    notifempty
    create 644 whit3rabbit staff
    postrotate
        # Signal any running processes to reopen log files
        pkill -USR1 -f "zigcat.*test" || true
    endscript
}
