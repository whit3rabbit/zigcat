/app/docker-tests/logs/*/*.log {
    size 100M
    rotate 10
    compress
    delaycompress
    missingok
    notifempty
    create 644 jules jules
    postrotate
        # Signal any running processes to reopen log files
        pkill -USR1 -f "zigcat.*test" || true
    endscript
}
